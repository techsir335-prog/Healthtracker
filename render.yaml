services:
  - type: web
    name: vitascore
    env: python
    buildCommand: "pip install -r requirements.txt && python ml_model/train_model.py"
    startCommand: "gunicorn wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: settings
